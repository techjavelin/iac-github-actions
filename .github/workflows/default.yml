---
name: Continuous Build

on:
  push:
    branches:
      - 'main'

jobs:
  terraform-validate:
    strategy:
      matrix:
        runner: [ 'ubuntu-latest' ]
        backend: [ 'local', 'terraform-cloud' ]
        test: [ 'simple' ]
    uses: techjavelin/iac-github-actions/.github/workflows/iac-github-actions-test-terraform-workflows.yml@${{ github.sha }}
    with:
      test-path: ${{ runner.temp }}/workflow-terraform-${{matrix.test-target}}
